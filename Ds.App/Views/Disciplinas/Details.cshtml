@model Ds.App.ViewModels.DisciplinaViewModel


@{
    ViewData["Title"] = "Detalhes da Disciplina - " + Model.NomeDisciplina;
}

<h4 style="padding-top: 50px">@ViewData["Title"]</h4>
<hr />

<div>

    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.NomeDisciplina)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.NomeDisciplina)
        </dd>
    </dl>
</div>
<div>
    <a class="btn btn-dark" asp-action="Edit" asp-route-id="@Model.Id">Editar</a>
    <a class="btn btn-info" href="javascript:window.history.back();">Voltar</a>
</div>
